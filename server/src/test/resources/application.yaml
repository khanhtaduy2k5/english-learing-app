spring:
  config:
    import: ""   # disable .env import in test context
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

groq:
  api:
    key: test-key-placeholder
    url: https://api.groq.com/openai/v1/chat/completions
  model: llama-3.3-70b-versatile

jwt:
  secret: testSecretKeyThatIsAtLeast32CharsLong1234567890
  expiration: 900000
  refreshExpiration: 604800000
